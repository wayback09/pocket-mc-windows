<ui:FluentWindow x:Class="PocketMC.Desktop.Features.Tunnel.CreateTunnelDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Create Tunnel"
        Width="520" Height="540"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowCornerPreference="Round" ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica" Background="{DynamicResource ApplicationBackgroundBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="✦ Create Tunnel"
                       FontSize="18" FontWeight="Bold"
                       Foreground="#89B4FA"
                       Margin="0,0,0,4"/>
            <TextBlock Text="Configure a new PlayIt.gg tunnel for your Minecraft server."
                       Foreground="#BAC2DE" FontSize="13"
                       TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Form Fields -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,0,12">
            <StackPanel>
                <!-- Tunnel Name -->
                <TextBlock Text="Tunnel Name"
                           Foreground="#CDD6F4" FontWeight="SemiBold" FontSize="13"
                           Margin="0,0,0,6"/>
                <TextBox x:Name="TxtTunnelName"
                         MaxLength="64"
                         FontSize="13"
                         Foreground="#CDD6F4"
                         Background="#313244"
                         BorderBrush="#45475A"
                         CaretBrush="#CDD6F4"
                         Padding="10,8"
                         ToolTip="ASCII characters only"/>
                <TextBlock x:Name="TxtNameError"
                           Foreground="#F38BA8" FontSize="11"
                           Margin="0,4,0,0"
                           Visibility="Collapsed"
                           TextWrapping="Wrap"/>

                <!-- Tunnel Type -->
                <TextBlock Text="Tunnel Type"
                           Foreground="#CDD6F4" FontWeight="SemiBold" FontSize="13"
                           Margin="0,16,0,6"/>
                <ComboBox x:Name="CmbTunnelType"
                          FontSize="13"
                          Foreground="#CDD6F4"
                          Background="#313244"
                          BorderBrush="#45475A"
                          SelectedIndex="0"
                          MinHeight="36">
                    <ComboBoxItem Content="Minecraft Java" Tag="minecraft-java"/>
                    <ComboBoxItem Content="Minecraft Bedrock" Tag="minecraft-bedrock"/>
                </ComboBox>

                <!-- Local Port -->
                <TextBlock Text="Local Port"
                           Foreground="#CDD6F4" FontWeight="SemiBold" FontSize="13"
                           Margin="0,16,0,6"/>
                <TextBox x:Name="TxtLocalPort"
                         Text="25565"
                         MaxLength="5"
                         FontSize="13"
                         Foreground="#CDD6F4"
                         Background="#313244"
                         BorderBrush="#45475A"
                         CaretBrush="#CDD6F4"
                         Padding="10,8"
                         ToolTip="Port number between 1 and 65535"
                         PreviewTextInput="TxtLocalPort_PreviewTextInput"/>
                <TextBlock x:Name="TxtPortError"
                           Foreground="#F38BA8" FontSize="11"
                           Margin="0,4,0,0"
                           Visibility="Collapsed"
                           TextWrapping="Wrap"/>

                <!-- Region -->
                <TextBlock Text="Region"
                           Foreground="#CDD6F4" FontWeight="SemiBold" FontSize="13"
                           Margin="0,16,0,6"/>
                <ComboBox x:Name="CmbRegion"
                          FontSize="13"
                          Foreground="#CDD6F4"
                          Background="#313244"
                          BorderBrush="#45475A"
                          SelectedIndex="0"
                          MinHeight="36">
                    <ComboBoxItem Content="Global (default)" Tag="global"/>
                    <ComboBoxItem Content="North America" Tag="north-america"/>
                    <ComboBoxItem Content="Europe" Tag="europe"/>
                    <ComboBoxItem Content="Asia" Tag="asia"/>
                    <ComboBoxItem Content="India" Tag="india"/>
                    <ComboBoxItem Content="South America" Tag="south-america"/>
                    <ComboBoxItem Content="Japan" Tag="japan"/>
                    <ComboBoxItem Content="Australia" Tag="australia"/>
                </ComboBox>

                <!-- Enable immediately -->
                <Grid Margin="0,16,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="Enable immediately"
                                   Foreground="#CDD6F4" FontWeight="SemiBold" FontSize="13"/>
                        <TextBlock Text="The tunnel will start routing traffic as soon as it's created."
                                   Foreground="#A6ADC8" FontSize="11"
                                   Margin="0,2,0,0"/>
                    </StackPanel>
                    <ui:ToggleSwitch x:Name="ToggleEnabled"
                                     Grid.Column="1"
                                     IsChecked="True"
                                     VerticalAlignment="Center"/>
                </Grid>
            </StackPanel>
        </ScrollViewer>

        <!-- Inline error message area -->
        <Border Grid.Row="2"
                x:Name="ErrorBorder"
                Background="#3B1A26"
                BorderBrush="#F38BA8"
                BorderThickness="1"
                CornerRadius="6"
                Padding="12,8"
                Margin="0,0,0,12"
                Visibility="Collapsed">
            <TextBlock x:Name="TxtApiError"
                       Foreground="#F38BA8" FontSize="12"
                       TextWrapping="Wrap"/>
        </Border>

        <!-- Action Buttons -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Spinner (hidden by default) -->
            <ui:ProgressRing x:Name="SpinnerRing"
                             Grid.Column="0"
                             Width="20" Height="20"
                             IsIndeterminate="True"
                             Visibility="Collapsed"
                             Margin="0,0,12,0"
                             VerticalAlignment="Center"/>

            <ui:Button Grid.Column="2"
                       x:Name="BtnCancel"
                       Content="Cancel"
                       Width="100"
                       Click="BtnCancel_Click"
                       IsCancel="True"/>

            <ui:Button Grid.Column="4"
                       x:Name="BtnCreate"
                       Content="Create"
                       Appearance="Primary"
                       Width="120"
                       Click="BtnCreate_Click"
                       IsDefault="True"/>
        </Grid>
    </Grid>
</ui:FluentWindow>
